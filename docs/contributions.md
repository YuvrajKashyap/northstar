# Contribution history

Northstar is a team project. This note summarizes the repository history so the portfolio does not imply sole authorship.

## Yuvraj Kashyap

Commit history attributes Yuvraj's work primarily to:

- restoring and wiring the application routes and style layers after parallel hackathon changes;
- landing page, navigation, and authentication presentation;
- onboarding gating and user-specific memory/workspace behavior;
- scenario usefulness, goal mutations, and agent-surface integration;
- merge recovery and frontend build fixes.

Representative commits include `wire the routes back together`, `make the nav actually go places`, `Implement user-specific memory onboarding and dashboard polish`, `make scenarios actually useful`, `Sync workspace memory search and agent surfaces`, `Require onboarding before app access`, and `Enable agent chat goal mutations`.

## Kushagra Bharti

Commit history attributes Kushagra's work primarily to:

- initial repository and demo-seed setup;
- the first onboarding, memory graph, and application style system;
- foundational Supabase Auth/onboarding and local agent runner work;
- later workspace, graph, and markdown restoration;
- the original product demo deck.

Representative commits include `initialize project with package.json and demo seed script`, `Implement onboarding and memory graph features`, `Use Supabase auth and workspace onboarding`, `Implement local agent runner and dashboard workspace`, and `Add Northstar Product Demo Deck presentation`.

## Reading this note

This is a path-level summary of visible Git history, not a claim about off-repository design discussions, pair programming, or hackathon responsibilities that commits cannot prove. The [GitHub contributor graph](https://github.com/YuvrajKashyap/northstar/graphs/contributors) remains the source of record.
